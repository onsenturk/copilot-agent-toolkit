# CLAUDE.md

This repository uses a single set of cross tool agent instructions. Follow [AGENTS.md](AGENTS.md) for the always on rules (grounded answers, security, Azure safety, implementation discipline) and the governing standards it references.

For file scoped conventions, read the matching rule in [.github/instructions/](.github/instructions/) before editing a file, selecting it by the `applyTo` glob in its frontmatter (for example Bicep, PowerShell, shell, and markdown).

The full orchestration and workflow detail lives in [.github/copilot-instructions.md](.github/copilot-instructions.md).
